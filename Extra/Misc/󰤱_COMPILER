󰤱  fix ⋄ᵜ
󰤱  fix a, = [1] parsing as a,□ = ...
󰤱  Error handling
    https://ganer.xyz/s/6493fd709262abc8
    1. Format for sourcemaps
    2. Thing that concats trees so you can generate sourcemaps for multi-files
󰤱  add back the thing that did auto-imports on certain symbols like ¶
󰤱  ⬤>1>2
󰤱  ⨡ a ⨡ b ⨡ c
󰤱  escaping keywords with _ is dumb, like `del≡del_`
󰤱  make some vars auto-rerefference globals like 𝔒𝔘𝔗
󰤱  whitespace involution lol
󰤱  string ␛ before newline should not add it
󰤱  with statements (prob ⓦ under or something)
󰤱  store info abt stuff so i can say have a symbol that gives me the current line or line number
󰤱  replace multiple calls to ftrp with a thing that finds relevent nodes, groups them, then does things type by type; also make sure whenever u do a replcaement with some remaining you check if that type that the find condition for the latter replacements applies or not; if so, dont remove it from the 󰤱 list
󰤱  split up files into smaller asts for better threading
󰤱􊽨 deque for þSTK
󰤱  "joe"󷹝¹⁶ should give the charcodes in the base iasdjia
      for 􋇙 maybe use a 𐞑 or somethingiadfsj
󰤱  ∀a⬠bᐖ1:1  check for this type beat

 🐈‍⬛‍⬛‍⬛‍⬛‍⬛‍🟩‍🟫
All orders for all operators
Colors and number-labels of ⬤
  number for which slot in order so like (󰲣⌃󰲡)(2,3)≡3²
  color how far up it goes so if ⬤⫢purple, ⬤⋅3⋅~⬤+2 is 󰲣x⋅3+~y󰅂+2
  Wat if we dd the same thing for whickers and the balls?
  so like purple 󰲣 matches until something more patient than purple
  
  specifics of the following is a 󰤱, but it seems cool
  what if i had like symbols inside balls like (α) (β)
    and balls with symbols mean perform the outer func
    on the iterable thats the input and the same symbol
    means to zip together like ⟦⟧⁅⁆ in fontcompose
    
    (󰁎󷰭¹)☾       ⟶ aᴍ²☾       skipping syms adds levels
    (󰁌󷰭¹)²+(󰁌󷰭²) ⟶ a󱑼bꟿ󰲣x²+y  same sym zips
    (󰁌󷰭¹)²+(󰁍󷰭²) ⟶ a⨉bꟿ󰲣x²+y  diff sym cross
    (󰁍󷰭¹)²+(󰁍󷰭²) ⟶ a󱑼bᐵꟿ󰲣x²+y  sym skip add level to outside?
    (󰁌󷰭²)²+(󰁌󷰭³) ⟶ a󱑼bᐵꟿ󰲣x²+y ?
    
    
    (󰁌󷰭¹)²+(󰁌󷰭²) is 󰲣x󱑼yꟿ󰲣x²+y
    (󰁌󷰭¹)²+(󰁍󷰭²) is 󰲣x⨉yꟿ󰲣x²+y
    (󰁌󷰭¹)² is 󰲡x²
    
    󰁎󷰭¹    : xᴍ²ƒ
    󰁌󷰭¹ 󰁌󷰭²: a󱑼bꟿ󰲣ƒ
    󰁌󷰭¹ 󰁍󷰭²: a⨉bꟿ󰲣ƒ
    󰁌󷰭¹ 󰁎󷰭²: bᴍx↦(a⨉xꟿ󰲣ƒ) ?
    󰁌󷰭¹ 󰁍󷰭¹: aᴍx↦a󱑼xꟿƒ ?
    󰁍󷰭¹ 󰁍󷰭²: a󱑼bꟿ²ƒ ?
    
    󰁌󷰭² 󰁌󷰭³: a󱑼b⛶􋌵ᵃꟿ󰲣ƒ
    
     num and sym skips should mean stuff
    
    [[a₀ ₀,a₀ ₁,…],[a₁ ₀,…]],b,[c₀,c₁,…]
    want: [ƒ(a ₀,b,c), ƒ(a ₁,b,c), …]
    
    ƒ(󰁍󷰨¹, 󷰨², 󰁌󷰨³)
    
    (󰁌󷰭¹) (󰁎󷰭²): a⨉bꟿ󰲣ƒ
    
    (󰁍󷰭¹)²+(󰀷󷰭²) is 󰲣x⨉y
    
     ‹xˣₓ􊾆􊿶􊫄􊬴 󷰠󷰭󷰜󷰝󷰞󷰟󷰠󷰡󷰢󷰣󷰤󷰥󷰦󷰧󷰨󷰩󷰪󷰫󷰬󷰭󷰮󷰯󷰷󷰰󷰱␀ ␀αβγΔεζ ␀123456›⍭⨉

þSTK: This a stack
      Each thread has its own stack
      󰤱 When making a thread it should prob copy it over
      þPSH = 󰲡push x onto þSTK and return x
      þPKE = 󰲡xth item on þSTK
      þPOP = 󰲡pop and returns xth item on þSTK
      þDEL = 󰲡delete x items from top of þSTK
      __þADDGLOBALS_CLEAN__ __þIMPORT__ __þADD_EXPORTS__
      __þGET_GLOB_MODNAME__ __þSET_GLOB_MODNAME__

For a transpiler:
    Must provide PEV and VEP functions that convert encode/decode var names to the lang
        (Ideally, easy to write a tool that find encoded names in a string and decodes them)

https://ganer.xyz/s/924c92e7b743b4d4